import actions from "@/store/device/actions";
import getters from "@/store/device/getters";
import setters from "@/store/device/setters";
import mutations from "@/store/device/mutations";
import state from "@/store/device/state";

export default {
    namespaced: true,
    state,
    actions,
    getters,
    setters,
    mutations
}